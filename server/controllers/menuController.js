// Placeholder menu controller for future updates
const getMenuItems = async (req, res) => {
  res.json({ success: true, message: 'getMenuItems placeholder' });
};

module.exports = {
  getMenuItems
};
